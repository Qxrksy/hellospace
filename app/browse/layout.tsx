export const metadata = {
  title: "Browse Members | Discover People on HelloSpace",
  description: "Browse and discover new members on HelloSpace. Find friends, connect with like-minded people, and explore the community.",
};

export default function BrowseLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
